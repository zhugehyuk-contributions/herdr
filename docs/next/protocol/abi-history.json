{
  "$comment": "APPEND-ONLY wire-ABI ledger for herdr-mx. One entry per (protocol_version, wire_abi_epoch) this fork has shipped, oldest first. Hand-maintained; checked by `abi_history_*` in src/protocol/wire_fixtures.rs. Adding an entry is the ONLY correct response to a red `wire_schema_fingerprint_is_pinned` — repinning the constant without appending here is the laundering the whole mechanism exists to prevent.",
  "rules": [
    "1. Existing fixture corpora are never overwritten. Only the LAST entry may point at docs/next/protocol/fixtures.json (the live, regenerated corpus); every earlier entry must point at a file under docs/next/protocol/fixtures/ whose sha256 is pinned here.",
    "2. A wire change bumps WIRE_ABI_EPOCH (src/protocol/abi.rs), repins WIRE_SCHEMA_FINGERPRINT, archives the outgoing fixtures.json under fixtures/, and appends an entry here. `fixtures_are_current` then forces the new corpus to be regenerated.",
    "3. Every entry states, explicitly, whether the current server still accepts that ABI. `accepted_by_current_server: true` MUST have a matching row in `abi::accepted_wire_abis()`; the two are compared by `abi_history_mirrors_the_accepted_table`. Silence is not a decision."
  ],
  "entries": [
    {
      "protocol_version": 20,
      "wire_abi_epoch": 0,
      "schema_fingerprint": null,
      "fixtures": "fixtures/protocol-20-epoch-0.json",
      "fixtures_sha256": "0375bb2294047679ca53783e2491415a1ccf6fcdbf022a3d982240e8d3daa315",
      "accepted_by_current_server": false,
      "decided_on": "2026-08-22",
      "decision": "REJECTED. Epoch 0 is the pre-prelude handshake: it announced no fork, no epoch and no schema fingerprint, so a peer speaking it cannot be identified at all. An unannotated Hello claiming protocol 20 is equally consistent with herdr-mx and with upstream/master, and those two disagree on ClientLaunchMode::TerminalAttach (1 vs 2) and ServerMessage::Terminal (13 vs 2) - mobile/.prd/03-blockers.md B1. Accepting epoch 0 would therefore mean guessing, and a wrong guess is a silent mis-parse rather than a loud rejection, which is exactly the failure M0-a was created to remove. Note the fingerprint field is null on purpose: epoch 0 builds never computed one, so recording a value here would be a claim about bytes nobody ever sent.",
      "user_impact": "A herdr client built before this change cannot connect to a server built after it, and vice versa. This is the same breakage class as any PROTOCOL_VERSION bump (11 of them in the 79 days to 2026-08-08), and it lands on the same, already-built recovery path: `validate_running_server_compatibility` (src/server/autodetect.rs) compares the running server's reported protocol before a socket is even opened and prints the session restart guidance. Rejection is also diagnosable on the socket itself - the server answers a prelude-less client with a legacy-framed Welcome carrying `abi::legacy_peer_rejection()`, so an old client prints a reason instead of dying on an unexplained close."
    },
    {
      "protocol_version": 21,
      "wire_abi_epoch": 1,
      "schema_fingerprint": "3ce2d29b371f9dbe",
      "fixtures": "fixtures/protocol-21-epoch-1.json",
      "fixtures_sha256": "778d63c09cf83bd74ab5b3cf80a90b1b0bfad84d68034b24a8c517fa48576092",
      "accepted_by_current_server": false,
      "decided_on": "2026-08-22",
      "decision": "REJECTED, and the reason is asymmetry rather than ambiguity - unlike epoch 0, an epoch-1 peer identifies itself perfectly. Epoch 2 only APPENDS ClientMessage::RetargetTerminal at tag 14, so an epoch-1 client talking to this server is genuinely safe: it never emits tag 14 and every ServerMessage is byte-identical. The reverse is not. An epoch-2 client (the mobile app is the whole reason this axis exists) sends RetargetTerminal on its first pane swipe, and an epoch-1 server has no tag 14 - it fails the bincode decode and drops the connection. Since check_peer_abi is the same table on both ends, accepting epoch 1 here would accept it in BOTH directions and buy a working old-client/new-server pair at the price of a new-client/old-server pair that dies on the first swipe instead of refusing to connect. There is no capability negotiation in Welcome to split the two, so the honest window is the empty one. Second, smaller reason: epoch 1 was introduced earlier the same day on this same unmerged branch and never shipped, so accepting it would be declaring support for a peer that does not exist anywhere.",
      "user_impact": "No user is affected: no epoch-1 build was ever released. If one is built from an intermediate commit of feat/mobile-client, it is refused on the prelude with the axis named ('incompatible wire ABI epoch: peer ... vs this build ...', src/protocol/abi.rs check_peer_abi), which is a diagnosable refusal rather than a mis-parse, and the existing pre-connection check (validate_running_server_compatibility, src/server/autodetect.rs) still prints the session restart guidance before a socket is opened."
    },
    {
      "protocol_version": 21,
      "wire_abi_epoch": 2,
      "schema_fingerprint": "827ef6800c3af3ee",
      "fixtures": "fixtures.json",
      "fixtures_sha256": null,
      "accepted_by_current_server": true,
      "decided_on": "2026-08-22",
      "decision": "ACCEPTED - this is the ABI this build speaks. Epoch 2 appends ClientMessage::RetargetTerminal {target, mode: TerminalSessionMode} at the enum TAIL (tag 14) so a mobile client can move an established observe session to another pane, and between observe and control, without a new ssh exec channel, a new handshake or a fresh ~56 KB uncompressed ANSI baseline (mobile/.prd/03-blockers.md B6, milestone M6). No existing wire tag moved and no existing payload changed - client_message_wire_tags_preserve_protocol_15_order still pins 0..13 and server_message_wire_tags_preserve_protocol_20_order is untouched - but wire_schema_fingerprint hashes an exemplar of every variant, so growing the enum moves the digest, and that is correct: the set of messages a peer may legally send IS its layout. PROTOCOL_VERSION deliberately stays at 21. The handshake's byte stream (28-byte prelude, then Hello, then Welcome) is unchanged, and M0-a's whole design is that the version names the negotiation while the epoch names the byte layout; bumping both for one layout change would put the two axes back into the single integer that B1 measured as dead.",
      "user_impact": "No behaviour change between a client and a server from this build: the desktop TUI, herdr attach, the observe/terminal-ANSI stream and the JSON API all put the same bytes on the wire as epoch 1. What changes is what a client MAY send. A client built at epoch 1 cannot connect to this server and vice versa; that refusal is the epoch-1 row above and lands on the same already-built recovery path as any PROTOCOL_VERSION bump."
    }
  ]
}
