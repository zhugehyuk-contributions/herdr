{
  "//": "Source-only project. `types: []` and the ES2020-only lib are the machine check that the codec never reaches for a Node/DOM API — it has to run on Hermes. Tests type-check under tsconfig.test.json, which is the only place Node types exist.",
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "types": [],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["src"],
  "//exclude": "src/node is the Node-only entry (`@herdr/client-ts/node`, ssh2), kept out of the Hermes project. NOTE: this does NOT make tsc reject a leak — measured 2026-08-22, adding `export … from './node/index.js'` to src/index.ts still compiles clean here, because `types: []` only suppresses AUTOMATIC @types inclusion and @types/ssh2 (which pulls in node via a /// reference) is resolved on demand. The machine check for the leak is test/hermesContract.test.ts.",
  "exclude": ["src/node"]
}
