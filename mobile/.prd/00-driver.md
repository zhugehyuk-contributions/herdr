# 00 — 드라이버 (여기부터 읽는다)

**"herdr-mx/mobile 개발 진행해줘"를 받으면 이 파일만 읽고 다음 행동이 결정돼야 한다.**
결정이 안 되면 그건 이 파일의 결함이다 — 작업 끝에 여기를 갱신하는 것이 작업의 일부다.

---

## 목표 (유저 원문, 2026-08-23)

> 1. herdr 모바일 터미널 앱 (휴대폰으로 내 원격 터미널 에이전트 사용)
> 2. orca mobile 와꾸를 이용하여 대부분 모르겠으면 orca mobile에서 결정한 내용을 사용
> 3. android와 ios앱을 만들고 각각 qa 절차대로 컨펌 (반드시 별도 에이전트가 qa 완료 판정 해야함)

**닫힌 전제 — 재심의 금지.** ①네이티브 앱을 짓는다(TUI 레이아웃은 대안이 아니다) ②orca mobile 와꾸를
최대 재사용하고, 모르는 것은 orca의 결정을 따른다([`08-orca-port-map.md`](./08-orca-port-map.md))
③완료 = Android **와** iOS 둘 다, 각각 QA 컨펌.

**"됐다"의 정의가 바뀌었다.** 이전 마일스톤들은 "코드가 있다"에서 끝났다. 이제 각 마일스톤은
**해당 플랫폼에서 QA를 통과하고, 그 판정을 별도 에이전트가 내렸을 때** 닫힌다.

---

## 지금 상태 (2026-08-23)

코드는 M4까지 두껍게 있는데 **실기기 검증이 거의 없다.** 이게 현재의 진짜 병목이다.

| | 코드 | Android 검증 | iOS |
|---|---|---|---|
| M-1 지반 동기화 (upstream v0.8.2) | ✅ `be4d1505` mx 머지 | — | — |
| M0 와이어 ABI | ✅ prelude + 픽스처 | — | — |
| M1 `herdr-client-ts` | ✅ 15파일/173테스트 | — | — |
| M2 읽기 전용 앱 | ✅ | **✅ 5/5** | **FAIL** (§R D2) — 수리 중 |
| **M2b 원격 설정 + 키 보관** | ✅ | **✅ 3/3** | 판정 불가 — **서명 게이트** |
| M3 입력 | ✅ | **✅ PASS 4/4** | ❌ |
| M4 transport 생존성 | ✅ ssh redial 포함 (`845f0f22`) | ❌ | ❌ |
| M5 푸시 | **서버 절반 ✅** `81207036`+`9210e99d` · 앱 절반·센더 ❌ | — | — |
| M6 제어·스크롤·리타겟 | ❌ | ❌ | ❌ |

**M2 QA 완료 (2026-08-23, 별도 에이전트 판정).** 라이브 스크래치 서버 상대, 위조 불가 토큰 `17491`로 확증:
- **(b) PASS** 80컬럼 pane 육안 판독 · **(c) PASS** 30분 11초 관측, 15초 간격 121샘플 전부 `rect=214x51` 동일
- **(d) PASS** 백그라운드 10왕복, PID 불변, FATAL 0 · **(e) FAIL** ↓

**M2 Android 닫힘 (2026-08-23).** (e)는 `284c0558` 수리 후 **별도 에이전트 재판정에서 PASS** —
그리드가 213컬럼 pane의 모든 배율·2개 폰트스케일에서 `1630×777` 불변, 줌이 서버 프레임 20개/48초를
통과해 유지, 줌아웃이 매번 비트 단위로 fit 복귀, 최대 줌(7.92× fit)에서 커서 행 도달.
구현자가 우려했던 "최대 줌 세로 도달"도 두 손가락 드래그로 양 끝 도달 확인 — 다음 결함 아님.

**잔여 (e를 좁히는 선행 결함, §P)**: 213컬럼 중 **마지막 7~9열이 어떤 배율에서도 도달 불가**.
`cellW × cols = 1630 CSS` vs 실제 도색 폭 ~1700 CSS의 가로 전용 메트릭 불일치. 회귀 아님이 실측으로
확정됐다(수리 전후 클램프 산술 동일).

아래는 수리 전 판정 기록이다:

**(e)가 M2를 열어두고 있었다.** 서버 크롭은 통과(213컬럼 전량 도달)하는데, v1이 지정한 유일한 수동 이동
수단인 **핀치줌이 pane을 영구 손상**시킨다 — `terminal-webview-html.ts:279-299`가 핀치 종료 시
`term.resize(폰 뷰포트 기준 cols)`를 불러 그리드를 213→약 38로 로컬 리사이즈하는데 서버는 계속 213x51을
보내고, 되돌릴 경로가 없다(`onTextScaleChange={NOOP}`, `.reflow(` 호출자 0). 재마운트로만 복구.

수리(`284c0558`)의 요지: 핀치가 `term.resize`를 부르지 않고 **지속 배율**(`userScale`)로만 확대한다.
순진하게 `term.resize`만 지우면 안 되는 이유가 산술이다 — 그리드가 고정이면
`fit = vpWidth / (cellW × cols)`가 폰트 변경을 정확히 상쇄해서 **겉보기 글자 크기 = vpWidth / cols**가 된다.
넓은 pane에서 폰트 크기는 줌 손잡이가 될 수 없다. 그래서 배율이 프레임을 넘어 살아남아야 했다.
orca의 나머지 절반(PTY 리사이즈)이 부재한다는 것도 증명됐다 — `measureFitDimensions` 호출자 0.

### iOS 트랙 — 2026-08-23 열림

**Xcode 26.6 설치 확인** (`/Applications/Xcode.app`, Build 17F113). 이 트랙을 막던 유일한 유저 게이트가 풀렸다.

다만 **Xcode 설치만으로는 시뮬레이터가 생기지 않는다** — 설치 직후 `xcrun simctl list runtimes`가 비어 있고
디바이스 타입만 42종 있다. `xcodebuild -downloadPlatform iOS`로 런타임(iOS 26.5 Simulator, **8.52 GB**)을
따로 받아야 한다. 이걸 모르면 "Xcode 깔았는데 시뮬레이터가 0개"에서 막힌다.

진행 중: prebuild(`mobile/ios/` 생성) + 런타임 다운로드 병렬.

**iOS 첫 실행 전에 알아둘 것** — Android 전례가 그대로 적용된다: `assembleDebug`가 green인데
**Expo 모듈 등록이 실행 시점에 던졌다**(`.prd/06:165`, `Class()`에 `Constructor` 필수). 컴파일 통과를
성공으로 읽지 마라. iOS도 같은 등급의 결함이 실행에서만 나올 수 있다.

---

## 다음 행동

순서는 "막힌 것을 먼저 알리고, 안 막힌 것을 끝까지"다.

1. ~~**[유저] Xcode 설치**~~ — ✅ 2026-08-23 완료(Xcode 26.6). 결정 7(ssh 라이브러리 핀)도 **확정됨**:
   `orlandos-nl/Citadel` `exactVersion 0.12.1` (`ae8562f8`) + `Wellz26/swift-nio-ssh` **revision** `a05e6bb`.
   `npm run typecheck:ios`가 실제로 그 조합을 컴파일한다(exit 0 실측). 설치되면 바로 `expo prebuild`로 간다.

   > **iOS는 사실상 ed25519 전용이다** — Citadel의 `OpenSSHKey.swift:307-313`이 `BEGIN OPENSSH PRIVATE KEY`
   > 컨테이너만 받고, 파싱을 통과한 RSA도 서명에서 죽는다(`rsa-sha2`가 Citadel 0.12.1·nio-ssh 0.3.6에 **0회**,
   > 서명 프리픽스가 `ssh-rsa` 하드코딩 — OpenSSH 8.8이 SHA-1 RSA를 껐다). Android가 `loadKeys` 포맷 판별로
   > 고전 PEM RSA까지 받는 것과 **정확히 반대 방향의 제약**이다.
   > **유저 키 실측(2026-08-23)**: `~/.ssh/*.pub` 8개 중 7개가 `ssh-ed25519`, 주력(`id_ed25519`,
   > `id_ed25519-z-iq`)이 openssh-key-v1 ed25519 → **실사용에 지장 없다.** RSA 1건(`perplay-vm_key`)만
   > iOS에서 못 쓴다. 차단 아님, 문서화 대상.
2. **[에이전트] Android M2 수용 (b)~(e) 완주** — 코드는 이미 있다. 필요한 건 격리 스크래치 서버 + 실행.
   `.prd/10`의 절차, 판정은 별도 에이전트.
3. ~~**M4 ssh redial**~~ — ✅ `845f0f22`. 재다이얼 판정은 ①채널 개설 거부 시 즉시 ②`Welcome` 미도달 3연속.
   타이머 없이 L3 사다리 속도를 그대로 따라간다. 라이브 스위트가 TCP 프록시 accept로 실제 다이얼을 셌다(`1→5`).
   **잔여**: 마운트 시 실패한 원격은 여전히 앱 수명 내내 죽어 있다(§L1의 나머지 절반).

3b. ~~**M2b 원격 설정 + 키 보관**~~ — ✅ 코드 `c22d26ae`, **Android QA PASS** (별도 에이전트).
   ①재빌드 없이 추가·다이얼(앱 프로세스 무중단, `4 nodes/demo data` → `1 nodes`)
   ②키 무잔존(logcat 51,403줄 전 구간 0건, 저장 형태는 Android Keystore AES 암호문)
   ③앱 삭제 시 소멸 + **purge 경로 강제 검증**(AsyncStorage만 지워 iOS 조건 재현 → 항목 삭제 확인).
   구현자가 "유닛 불가"로 넘긴 #1이 **sshd 로그로 닫혔다** — `Accepted publickey … ED25519 SHA256:M3qh…`가
   일회용 키 지문과 일치, 즉 sshj가 키체인에서 온 키를 실제로 받았다.
   **잔여(수용 기준 아님, 사용성)**: 키 필드를 탭해도 폼이 키보드 위로 자동 스크롤하지 않는다.
   **미검증**: 실제 클립보드 붙여넣기, 릴리즈 빌드의 `__DEV__=false` 경로.

3c. (구 3b 근거)
   **릴리즈 빌드에서는** 원격을 바꾸려면 `app.json`을 고치고 재빌드해야 하고, 그러면 개인키가 번들에 구워진다.
   (dev-client 디버그 빌드에서는 `app.json` 편집 + Metro 리로드로 바뀐다 — QA가 APK mtime 불변으로 실증했다.
   `cf9c5398` 커밋 메시지가 이 구분 없이 "재빌드해야 한다"고 쓴 것은 틀렸다.)
   2026-08-23 QA 랩 설정만으로 워킹트리에 평문 키가 올라왔다(커밋은 안 됐다). 정본은 `expo-secure-store` —
   `remote-config.ts:12`가 이미 그렇게 적어놨고 자료구조도 준비돼 있다. 보관처와 UI만 없다.
4. ~~**M3 입력 검증**~~ — ✅ **PASS 4/4** (별도 에이전트, `qa-m3` 고정 워크트리). 액세서리 키는
   **바이트 단위**로 판정: `1b 09 0d 20 1b5b43 03 04 0c 1a 1b5b44 1b5b41` = Esc/Tab/Enter/Space/→/^C/^D/^L/^Z/←/↑.
   Ctrl-C가 `sleep 300`을 실제로 죽이는 것까지 확인. PTY 39×93 불변, `resize` 이벤트 0건.
   수용 본문(승인 프롬프트 y)은 **스트림이 살아있는 상태에서 재진입 없이** 반영되는 것으로 판정.
   **여기서 §Q가 나왔다** — observe 스트림이 끊긴 뒤 화면을 열어둔 채로는 2분 넘게 재부착 안 됨.
5. **[에이전트] M4 생존성 실기기** — 기내모드 ×10 / Wi-Fi↔셀룰러 ×10 / 30분 백그라운드.
6. **M5 푸시 — 서버 절반은 이미 있었다.** 이 표가 "미착수"라고 적고 있던 것이 틀렸다: `81207036`이
   훅·센더·리시트 1472줄을 착륙시켰는데 **어떤 게이트도 그걸 실행하지 않았다**(`grep -rn blocked-push`
   플러그인 밖 0건). `9210e99d`가 22테스트를 `just ci`에 배선하고 격리 서버로 실발화를 확인했다
   (9회 훅 호출, 큐에 blocked 2건, 네트워크 차단 샌드박스).
   **남은 것**: ①앱 절반(`expo-notifications`, 이미 의존성) ②센더 데몬(현재 `file`/`command`/`null`
   어댑터만, 푸시 서비스 미배선). P2 coalesce는 센더 몫으로 확정됐다 — 봉투에 이전 상태가 없어
   훅이 물리적으로 못 한다.
7. **M6** — 마지막. 서버 변경을 동반한다.

**병렬 금지 아님**: 2·3·4는 서로 독립이라 동시에 굴려도 된다. per-unit dispatch(rules/DEV.md §3) 유지.

---

## QA 게이트 — 자기 인증 금지

> **QA 완료 판정은 반드시 별도 에이전트가 내린다** (2026-08-23 유저 지시).
> 구현한 에이전트도, 디스패처도 자기 작업의 QA를 통과시킬 수 없다.

절차 = [`10-mobile-qa.md`](./10-mobile-qa.md). 디스패처가 하는 일은 **QA 에이전트를 띄우고 그 판정을
받는 것**이지 판정 자체가 아니다. QA 에이전트는:

- 구현 에이전트와 **다른 디스패치**여야 한다
- `.prd/10`의 보고 형식 5항목을 채운다 (APK 시각+커밋 / 스크린샷과 읽은 내용 / mock·live 판정 근거 /
  열어본 화면 / 못 한 것)
- **PASS / FAIL을 명시**한다. "문제 없어 보임"은 판정이 아니다

FAIL이면 그 마일스톤은 안 닫힌다. 재구현 후 **다시 별도 에이전트**에게 보낸다.

---

> ⚠️ **이 표를 믿기 전에 확인하라.** 2026-08-23에 이 표가 M5를 "미착수"로 적고 있었는데 실제로는
> 1472줄이 이미 착륙해 있었다. 코드 존재 여부는 `git log`/`grep`으로, **게이트가 그걸 실행하는지는
> `justfile`/`ci.yml` 참조로** 따로 확인하라 — 이 레포에서 둘은 자주 어긋난다.

## 작업 종료 시 할 일 (이 파일의 수명 유지)

1. 위 상태 표 갱신 — 근거 커밋/스크린샷을 함께
2. "다음 행동" 재정렬
3. 새로 드러난 결함은 [`09-review-followups.md`](./09-review-followups.md)에 append
4. 계획 자체가 틀렸으면 **해당 문서를 고친다** — 드라이버에 예외를 쌓지 말고

---

## 문서 지도

| | |
|---|---|
| [`01-spec.md`](./01-spec.md) | 무엇을 만드는가 · 작업 환경 함정 |
| [`02-architecture.md`](./02-architecture.md) | 2채널 구조, 계층 |
| [`03-blockers.md`](./03-blockers.md) | B1~B10 — 왜 어떤 것이 불가능한가 |
| [`04-milestones.md`](./04-milestones.md) | M-1~M6 정의와 수용 기준 |
| [`05-orca-transport.md`](./05-orca-transport.md) | 생존성 L1~L6 (orca 이식) |
| [`06-open-decisions.md`](./06-open-decisions.md) | 미결 결정 + 실기기 검증 결과 |
| [`07-research-2026-08-22.md`](./07-research-2026-08-22.md) | 배포 채널·존재 조건 리서치 |
| [`08-orca-port-map.md`](./08-orca-port-map.md) | **와꾸 SSOT** — 모르면 여기서 orca의 결정을 본다 |
| [`09-review-followups.md`](./09-review-followups.md) | 리뷰 지적과 그 처리 원장 |
| [`10-mobile-qa.md`](./10-mobile-qa.md) | QA 절차 (판정은 별도 에이전트) |
